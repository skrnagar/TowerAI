Tower AI — dataset readiness check

  train: 783 images, 0 label files
    BLOCKER: images exist but no labels in dataset/labels/train/
  val: 0 images, 0 label files
  test: 6 images, 0 label files
    BLOCKER: images exist but no labels in dataset/labels/test/

Fix blockers above before training.
